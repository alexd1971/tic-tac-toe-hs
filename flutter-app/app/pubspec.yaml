name: tic_tac_toe_app
description: "Flutter UI for the Haskell tic-tac-toe core."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  tic_tac_toe_bridge:
    path: ../bridge

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
