name: tic_tac_toe_bridge
description: Flutter FFI package that bridges Dart and the Haskell tic-tac-toe core.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0
